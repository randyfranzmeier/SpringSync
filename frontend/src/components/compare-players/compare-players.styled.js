import styled from 'styled-components';

export const NoThreadsPlayerWrapper = styled.div`
    text-align: center;
`;

export const PlayersWithThreadsWrapper = styled.div`
    border-top: 2px solid black;
`;
