import React from 'react';
import {NavWrapper, NavBar} from './nav.styled';

const Nav = () => (
    <NavWrapper>
        <NavBar>
            SpringSync
        </NavBar>
    </NavWrapper>
);

Nav.propTypes = {};


export default Nav;
