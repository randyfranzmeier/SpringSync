import React from 'react';
import {NavWrapper, NavBar} from './nav.styled';

const Nav = () => (
    <NavWrapper>
        <NavBar>
            Concurrency Simulator
        </NavBar>
    </NavWrapper>
);

Nav.propTypes = {};

Nav.defaultProps = {};

export default Nav;
